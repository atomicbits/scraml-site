# Scraml documentation generation

Generate the documentation with:  

```
asciidoctor documentation.adoc
```

Install asciidoctor and pygments:

```
sudo gem install asciidoctor
sudo gem install pygments.rb
```

